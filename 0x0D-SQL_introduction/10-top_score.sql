-- a scriptthat lists all rows of the table second_table
-- from the database htbn_0c_0
-- results displays scoreamd name
-- ordered by score
SELECT score, name FROM second_table ORDER BY score DESC;
